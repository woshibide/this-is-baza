interface:
  display_name: "Baza"
  short_description: "Standard sketch workspaces with reusable UI controls"
  default_prompt: "Use $baza to give this sketch the standard workspace with reference controls, saved state, an aspect-correct preview, and working exports while preserving its artwork."
